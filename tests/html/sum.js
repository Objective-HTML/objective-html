module.exports = {
    print: function(message) {
        console.log(message)
    },
    split: function(text, word) {
        text.split(word)
    },
    join: function(text) {
        text.join('')
    }
}